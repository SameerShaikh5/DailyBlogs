 
        <!-- Navigation-->
        <nav class="navbar navbar-expand-lg navbar-light" id="mainNav">
            <div class="container px-4 px-lg-5">
                <a class="navbar-brand" href="/">Start Bootstrap</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                    Menu
                    <i class="fas fa-bars"></i>
                </button>
                <div class="collapse navbar-collapse" id="navbarResponsive">
                    <ul class="navbar-nav ms-auto py-4 py-lg-0">
                        <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="/">Home</a></li>
                        <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="/posts">All Posts</a></li>
                        <% if(isloggedin){ %>
                            <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="/users/logout">Logout</a></li>
                        <% } else{ %>
                            <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="/users/register">Register</a></li>
                            <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="/users/login">Login</a></li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </nav>

        <% if (message && message.length > 0) { %>
                <% const { text, type } = message[0]; %>
                <div class="alert alert-<%= type %> alert-dismissible fade show" role="alert" id="autoDismissAlert">
                    <%= text %>
                    <button type="button" class="btn-close ml-3 btn-sm" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
        <% } %>

        